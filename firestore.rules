rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read access to team collection
    match /team/{document} {
      allow read: if true;
    }
  }
} 